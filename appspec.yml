version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/kimby-playground/dist
file_exists_behavior: OVERWRITE

permissions:
  - object: /
    owner: ec2-user
    group: ec2-user
    mode: 755
